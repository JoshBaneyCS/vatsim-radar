version: '3.8'

services:
  tracker:
    build: .
    image: vatsim-tracker
    container_name: vatsim-tracker
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./branding:/app/branding:ro
    stdin_open: true
    tty: true
